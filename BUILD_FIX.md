# 🔧 Решение проблемы со сборкой EXE

## Проблема

Ошибка при сборке:
```
ERROR: Cannot create symbolic link
```

Это происходит потому что Windows требует права администратора для создания символических ссылок.

## Решение 1: Запуск от имени администратора (БЫСТРО)

1. **Закройте текущее окно PowerShell**
2. **Нажмите Win + X**
3. **Выберите "Windows PowerShell (Администратор)"**
4. **Перейдите в папку:**
   ```powershell
   cd C:\Users\xaxxa\OneDrive\Desktop\LynchFM\broadcaster
   ```
5. **Запустите сборку:**
   ```powershell
   npm run build:win
   ```

## Решение 2: Включить Developer Mode (ПОСТОЯННО)

1. **Откройте Настройки Windows** (Win + I)
2. **Обновление и безопасность** → **Для разработчиков**
3. **Включите "Режим разработчика"**
4. **Перезапустите PowerShell**
5. **Попробуйте сборку снова**

## Решение 3: Использовать portable версию без установщика

Я уже обновил `package.json` чтобы пропустить код-сигнатуру. Попробуйте снова:

```powershell
npm run build:win
```

Если не работает, попробуйте собрать только portable версию:

1. Откройте `broadcaster/package.json`
2. Найдите секцию `"win": {`
3. Временно замените на:
   ```json
   "win": {
     "target": "portable",
     "arch": ["x64"]
   }
   ```
4. Запустите: `npm run build:win`

## Решение 4: Очистка кеша

```powershell
# Очистите кеш electron-builder
Remove-Item -Recurse -Force "$env:LOCALAPPDATA\electron-builder\Cache"

# Попробуйте сборку снова
npm run build:win
```

## После успешной сборки

Файлы будут в папке `broadcaster/dist/`:
- **Установщик:** `LynchFM-Broadcaster-1.0.0-x64.exe`
- **Portable:** `LynchFM-Broadcaster-1.0.0-x64-portable.exe` (можно запускать без установки)

---

**Рекомендация: Используйте Решение 1 (запуск от администратора) - самое быстрое!**

