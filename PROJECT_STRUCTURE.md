# Структура проекта LynchFM

## 📁 Обзор структуры

```
lynchfm/
│
├── 📄 server.js              # Основной сервер Node.js
├── 📄 package.json           # Зависимости и скрипты сервера
├── 📄 env.example            # Пример переменных окружения
├── 📄 Dockerfile             # Конфигурация Docker
├── 📄 docker-compose.yml     # Docker Compose конфигурация
├── 📄 Procfile               # Конфигурация для Heroku
│
├── 📁 public/                # Веб-сайт радиостанции
│   ├── index.html           # Главная страница (полный сайт)
│   ├── style.css            # Профессиональные стили
│   └── app.js               # Клиентский JavaScript
│
├── 📁 broadcaster/          # Приложение для вещания (Electron)
│   ├── main.js              # Electron main процесс
│   ├── renderer.js          # Electron renderer процесс
│   ├── index.html           # Интерфейс приложения
│   ├── style.css            # Стили приложения
│   ├── package.json         # Зависимости приложения
│   └── README.md            # Документация приложения
│
└── 📁 docs/                 # Документация
    ├── README.md            # Основная документация
    ├── DEPLOY.md           # Руководство по развертыванию
    ├── QUICKSTART.md       # Быстрый старт
    └── PROJECT_STRUCTURE.md # Этот файл
```

## 🎯 Основные компоненты

### Backend (server.js)
- Express сервер
- Socket.io для WebSocket соединений
- WebRTC для передачи аудио
- API endpoints для статуса и статистики
- Безопасность (Helmet, CORS, Rate Limiting)

### Frontend (public/)
- Профессиональный веб-сайт
- Веб-плеер для прослушивания
- Визуализация аудио
- Адаптивный дизайн
- Статистика в реальном времени

### Broadcaster App (broadcaster/)
- Electron приложение
- Захват аудио (микрофон/системный звук)
- Визуализация аудио
- Управление информацией о треках
- Подключение к серверу через Socket.io

## 🔧 Конфигурационные файлы

### package.json (корень)
Основные зависимости сервера:
- express - веб-сервер
- socket.io - WebSocket
- dotenv - переменные окружения
- helmet - безопасность
- compression - сжатие ответов

### broadcaster/package.json
Зависимости приложения:
- electron - фреймворк для десктоп приложений
- socket.io-client - клиент Socket.io

### env.example
Пример переменных окружения:
- PORT - порт сервера
- HOST - хост сервера
- ALLOWED_ORIGINS - разрешенные домены
- NODE_ENV - режим работы

## 🚀 Скрипты

### Основные команды:
```bash
npm start          # Запуск сервера
npm run dev        # Запуск в режиме разработки
npm run install:all # Установка всех зависимостей
```

### Broadcaster:
```bash
cd broadcaster
npm start          # Запуск приложения
npm run build      # Сборка для всех платформ
npm run build:win  # Сборка для Windows
npm run build:mac  # Сборка для macOS
npm run build:linux # Сборка для Linux
```

## 📊 Потоки данных

1. **Диджей → Сервер → Слушатели**
   - Диджей подключается через Broadcaster App
   - Аудио передается через WebRTC
   - Сервер транслирует всем слушателям

2. **Веб-сайт**
   - Слушатели подключаются через веб-сайт
   - Получают аудио поток через WebRTC
   - Видят статистику в реальном времени

3. **API**
   - `/api/status` - статус станции
   - `/api/stats` - статистика

## 🔐 Безопасность

- Helmet.js для HTTP заголовков
- CORS настройки
- Rate limiting
- Валидация входных данных
- Защита от XSS и CSRF

## 📦 Развертывание

Поддерживаемые платформы:
- VPS сервер (Ubuntu/Debian)
- Docker
- Heroku
- Railway
- Render
- Любой Node.js хостинг

Подробности в [DEPLOY.md](./DEPLOY.md)

