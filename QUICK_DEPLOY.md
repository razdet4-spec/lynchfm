# ⚡ Быстрый деплой на бесплатный хостинг за 5 минут

## 🚀 Render.com (Самый простой способ)

### Шаг 1: Загрузите код на GitHub (2 минуты)

Если у вас еще нет репозитория:

```bash
# В папке проекта
git init
git add .
git commit -m "Deploy to Render"
```

Создайте репозиторий на GitHub:
1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например, `lynchfm`)
3. Не добавляйте README, .gitignore или лицензию

Загрузите код:
```bash
git remote add origin https://github.com/ВАШ_ЛОГИН/lynchfm.git
git branch -M main
git push -u origin main
```

### Шаг 2: Деплой на Render (3 минуты)

1. **Регистрация:**
   - Перейдите на https://render.com
   - Нажмите "Get Started for Free"
   - Войдите через GitHub

2. **Создание сервиса:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш репозиторий `lynchfm`
   - Выберите ветку `main`

3. **Настройки:**
   ```
   Name: lynchfm-radio
   Environment: Node
   Region: Frankfurt (или ближайший к вам)
   Branch: main
   Root Directory: (оставьте пустым)
   Build Command: npm install
   Start Command: npm start
   Instance Type: Free
   ```

4. **Переменные окружения:**
   Нажмите "Advanced" и добавьте:
   ```
   NODE_ENV = production
   PORT = 10000
   HOST = 0.0.0.0
   ALLOWED_ORIGINS = https://lynchfm-radio.onrender.com
   ```
   (URL изменится после создания, можете обновить позже)

5. **Создание:**
   - Нажмите "Create Web Service"
   - Дождитесь деплоя (5-10 минут)

6. **Готово!**
   - Ваш сайт будет доступен на `https://lynchfm-radio.onrender.com`
   - Скопируйте этот URL для использования в программе Broadcaster

---

## 📱 Тестирование

1. Откройте сайт на телефоне:
   - Введите URL в браузере
   - Нажмите кнопку воспроизведения

2. Откройте сайт на ПК:
   - Введите URL в браузере
   - Должно работать также

---

## 🎙️ Использование в Broadcaster

1. Откройте программу LynchFM Broadcaster
2. В поле "URL сервера" введите:
   ```
   https://lynchfm-radio.onrender.com
   ```
   (или ваш URL после деплоя)

3. Выберите источник звука и начните трансляцию

---

## ⚠️ Важно знать

### Render.com бесплатный tier:
- ✅ Полностью бесплатно
- ✅ Автоматический SSL
- ⚠️ Приложение "засыпает" после 15 минут бездействия
- ⚠️ Первый запрос после сна - задержка ~30 секунд
- ✅ Для постоянной работы: платный план $7/месяц

### Как избежать "сна":
1. Используйте сервис для пинга (например, UptimeRobot)
2. Настройте пинг каждые 10 минут
3. Или перейдите на платный план

---

## 🔄 Обновление сайта

После изменений в коде:

```bash
git add .
git commit -m "Update"
git push
```

Render автоматически задеплоит обновления!

---

## 🆘 Проблемы?

### Сайт не загружается:
- Подождите 30 секунд (возможно "спит")
- Проверьте логи в панели Render

### Не работает трансляция:
- Убедитесь что URL правильный в Broadcaster
- Проверьте что сервер запущен (статус в Render)

### Мобильная версия не работает:
- Проверьте что используете HTTPS (не HTTP)
- Попробуйте другой браузер

---

**Готово! Ваша радиостанция работает! 🎵**

